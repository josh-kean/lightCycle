import Car

playerNames = []
playerScores = {}
playerColors = []

def createPlayer(playerName):
    playerScores.append(playerName:0)
    playerNames.append(playerName)
    # create function to randomly generate a color and push it to playerColors
    #function also checks to make sure random color isn't in list of playerColors

def declareWinner(playerName):
    playerNames[playerName] +=1

def detectCrash()
